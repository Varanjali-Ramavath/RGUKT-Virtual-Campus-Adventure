name: your_project_name

description: A new Flutter project.

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.1
  provider: ^6.0.0
  hive: ^2.0.0
  lottie: ^1.2.0
  google_fonts: ^2.1.0
  audioplayers: ^0.20.0
  dio: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
